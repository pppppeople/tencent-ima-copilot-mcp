# IMA 登录态更新操作卡片

适用场景：微信里 `扣子问知识库：...` 返回认证失败、无有效内容，或 IMA MCP 查询开始报登录态过期。

## 最短流程

1. 打开 IMA 网页并确认已登录。
2. 打开目标知识库，例如「西风的罗盘」。
3. 使用之前安装的 IMA 登录态助手书签。
4. 在 IMA 里随便问一句，让网页发出请求。
5. 如果弹窗提示成功，本机接收器会自动更新 `.env` 并重启 MCP。
6. 回到微信发：

```text
扣子问知识库：测试一下当前知识库是否可用
```

## 如果书签不可用

打开本机安装页：

```text
http://127.0.0.1:8787/install
```

重新复制页面里的书签代码，拖到浏览器书签栏。

## 手动备用流程

1. 打开 IMA。
2. 打开浏览器开发者工具 Network。
3. 在 IMA 中问一句。
4. 找到 `/cgi-bin/assistant/qa` 或相关 POST 请求。
5. 复制请求头里的：

```text
x-ima-bkn
x-ima-cookie
```

6. 发给微信助手，消息里要包含 `IMA` / `知识库` / `登录态` / `请求头` 等关键词。

示例：

```text
扣子 IMA 登录态更新：
x-ima-bkn: ...
x-ima-cookie: ...
```

微信助手会自动打码记录，并调用：

```text
./update-and-restart.sh
```

## 当前知识库

```text
西风的罗盘
```

## 注意

- 认证信息属于敏感数据，不要提交到 git。
- IMA refresh key 过期后，不能稳定后台静默续期，必须从已登录网页重新提取。
- 如果更新后仍失败，先确认 IMA 网页本身能正常回答，再查 MCP 服务。
